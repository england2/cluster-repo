cluster-repo/cluster-config/cluster-yaml

This folder is for non-product related configuration. That is, the day-to-day configuration that keeps the cluster running.
Every folder in cluster-repo/cluster-config/cluster-yaml will be traversed, and all yaml will be reconciled.
